const reactLifecycleMessages = [
  "First, a React component is born when it mounts and injects itself into the browser's DOM.",
  "Next, the component renders its JSX to define exactly what the user sees on the screen.",
  "As users interact with the app, changes to state or props cause the component to update and re-render.",
  "Finally, a component dies and cleans up after itself when it unmounts from the DOM entirely.",
  "Developers use hooks like useEffect to trigger specific actions at different stages of this lifetime.",
];


console.log(reactLifecycleMessages);
