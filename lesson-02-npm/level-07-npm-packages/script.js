import chalk from "chalk";

console.log(chalk.green("The `node_modules` folder contains code from external modules. - Your project imports modules by package name from `node_modules`. Example: `import chalk from 'chalk' - In projects with `package.json`, the `import` statement can only be used with 'type': 'module'"))