const colors = ["red", "blue", "violet", "indigo", "yellow", "orange", "green", "cyan", "slateblue", "seagreen"];
for(let i = 0; i < colors.length; i++) {
    console.log(`color #${i + 1}: ${colors[i]}`)
}