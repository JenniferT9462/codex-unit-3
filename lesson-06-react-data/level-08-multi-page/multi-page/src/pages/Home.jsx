export default function Home() {
    return (
      <main>
        <p>
          To create a multi-page website with <code>react-router</code>, first you will need to install react router to your project by running 'pnpm install react-router' and import its components (BrowserRouter, Routes, Route), you
          wrap your application in a <code>BrowserRouter</code>, define your
          paths using <code>Routes</code> and <code>Route</code> components, and map each route to a specific page
          element. Next, build a navigation UI inside the <code>Header</code> component and use the paths from the Route tags for the <code>href</code> inside of the <code>anchor</code> tags. 
        </p>
      </main>
    );
}