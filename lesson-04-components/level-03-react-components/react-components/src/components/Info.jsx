export default function Info() {
    return (
      <p>
        React components are the essential building blocks of a web
        application's user interface. Think of them like reusable LEGO bricks:
        each component is a self-contained piece of code that handles its own
        layout and logic (like a button, a navbar, or a form). To use them, you
        write them like custom HTML tags—for example, &lt;MyComponent
        /&gt;—allowing you to easily piece together complex layouts while
        keeping your code organized and easy to maintain.
      </p>
    );
}