export default function Main() {
    return (
      <main>
        <p>
          Making a semantic template means structuring your layout using HTML
          elements that explicitly describe their purpose to both the browser
          and assistive technologies. Instead of building a page entirely out of
          generic, meaningless div tags, you intentionally use dedicated
          containers like header, nav, main, section, and footer. This
          architecture makes your code vastly more readable for other
          developers, drastically improves accessibility for screen readers, and
          boosts your site's SEO performance because search engines can easily
          identify where the core content lives.
        </p>
      </main>
    );
}